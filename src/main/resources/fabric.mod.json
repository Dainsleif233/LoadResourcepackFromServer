{
  "schemaVersion": 1,
  "id": "loadresourcepackfromserver",
  "version": "${mod_version}",
  "name": "LoadResourcepackFromServer",
  "description": "Have the client download the resource pack directly from the game server.",
  "authors": [ "Dainsleif" ],
  "contact": {},
  "license": "GPL-3.0",
  "environment": "server",
  "entrypoints": {
    "main": [
      "top.syshub.loadResourcepackFromServer.LoadResourcepackFromServerFabric"
    ]
  },
  "mixins": [
    "loadresourcepackfromserver.mixins.json"
  ],
  "depends": {
    "minecraft": ">=${minecraft_version}"
  }
}
